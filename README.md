# Type Enforcer

Библиотека для проверки использования кастомных типов данных в Python. Помогает стандартизировать типы в проекте, например, заменять `int` на `Int`, `float` на `Float` и т.д.

## Установка

```bash
pip install type-enforcer